# D12 验收 — 2026-09-11

本地 macOS arm64 开发验收。[English 与完整复现命令](d12-qa.md)。

| 工作流 | Forge 原生引擎 | Codex | 结果检查 |
| --- | --- | --- | --- |
| 代码修改 | DeepSeek v4 flash 真实执行，将减法修正为加法 | 已认证 Codex 真实执行相同用例 | 基线审查确认 sum.js 修改 |
| 本地文件 | 真实生成 CSV，保留 001/002，合计 30 | 相同断言通过 | 表格预览、导出字节一致 |
| 来源研究 | 随应用提供的 web-tools 真实搜索/抓取，中文报告 | Codex 真实运行生成英文来源报告 | 报告预览与导出通过 |

[Forge 原始记录](qa/d12/native.json)、[Codex 原始记录](qa/d12/codex.json)。
上述为 DesktopApplication 与 FileService 集成执行，不是 GUI 提交模型任务的录像。
Forge 本次出现 5 次审批，Codex 本次为 0 次；不代表 Codex 永久免审批。
Codex 报告中的抓取描述属于模型输出，桥接层没有 Forge 原生结构化来源字段或权威逐次补丁。
搜索摘要与正文分开描述，未宣称自动引用核验或双引擎完全等价。
原始 JSON 保留修复前的 CSV 截断标志，最终回归与界面检查验证了修复。

故障验收均为当日离线测试：Agent 退出后的请求清理；取消审批、断连和迟到事件；
目录丢失或边界变化；存储拒绝时保留快照；网络超时、取消、HTTP 失败且不切换服务；
损坏历史；会话并发写入与文件导入冲突；模型未配置或 Codex 不可用时失败且不回退。
完整测试文件与预期边界见英文版矩阵。这些测试不冒充真实供应商故障或生产进程崩溃。

隔离 Electron 验收使用明确标注的离线样例，经过真实 IPC 与文件服务，生成并检查
[10 张界面截图](qa/d12/ui/ui.json)：中英文代码、表格、研究、设置，以及 840 × 800 窄窗口。
没有页面横向溢出，差异区域内部滚动。窄窗口沿用隐藏详情栏的行为，需放宽窗口检查文件。
未对实时流式输出、审批弹窗和全部故障提示逐一做 GUI 验收。

修复任务切换残留旧文件预览/审查结果及异步返回覆盖问题，补全联网稳定错误码的本地化映射；
修复 PDF/CSV 请求范围超过文件末尾时误报截断，保留真实局部范围截断标记。
新增未配置模型与范围边界回归；界面脚本断言切换后旧结果清除。

验证通过：真实双引擎 2 项；聚焦 8 文件 / 69 项；确定性评估 13 文件 / 71 项；
`pnpm check`（两个已有信息提示）；桌面构建；Electron 界面脚本；
`pnpm package:verify` 的 CLI 实际打包安装（341291 字节）。`pnpm check:docs` 通过（162 个 Markdown 文件、610 个本地引用）。
Electron 有可选 canvas/DOMMatrix/Path2D 警告，本轮未验证 PDF 页面 GUI 渲染。

Brave 真实密钥、完整 GUI 故障状态矩阵、PDF GUI 渲染、安装后 GUI 的 PATH/proxy、
x64、签名、公证及发布仍未验证。安装环境交接属于 D13，CLI 安装验证不代表桌面安装验证。
本次未提交、签名或发布。
